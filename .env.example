# ── Camera ────────────────────────────────────────────────────────────────────
# RTSP stream URL, or use 0 for the default webcam
VIDEO_SOURCE=rtsp://user:password@192.168.1.100:554/stream

# ── Detection ─────────────────────────────────────────────────────────────────
# Confidence threshold (0.0 – 1.0)
CONFIDENCE=0.6

# ── Classes ───────────────────────────────────────────────────────────────────
# Must match the class order your model was trained with.
# Comma-separated names and hex colours (one per class).
CLASS_NAMES=Chicken 1,Chicken 2,Chicken 3,Chicken 4
CLASS_COLORS=#ef4444,#94a3b8,#3b82f6,#f59e0b

# ── Model ─────────────────────────────────────────────────────────────────────
# Path to the .pt file inside the container (mounted from ./models/)
MODEL_PATH=/app/models/best.pt
# 0 = detection model, 1 = segmentation model
MODEL_TYPE=0

# ── Server ────────────────────────────────────────────────────────────────────
# Host port for the web interface
HOST_PORT=80
